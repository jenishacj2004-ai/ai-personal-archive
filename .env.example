# Database Connection (MongoDB Atlas & SQLite local fallback)
MONGO_URI="mongodb+srv://<username>:<password>@cluster0.mongodb.net/personal_archiver?retryWrites=true&w=majority"
DATABASE_URL="file:./dev.db"

# JWT Secret for Session Authentication
JWT_SECRET="your-secure-jwt-secret-key"

# AI Provider API Keys (Optional - Intelligent fallback active if empty)
GEMINI_API_KEY="your-gemini-api-key"
OPENAI_API_KEY="your-openai-api-key"

# Application URL
NEXT_PUBLIC_APP_URL="http://localhost:3000"
